

<div class="text-center m-3">
  <h1 class="m-3">Let's add a new review</h1>
  <div class="container w-50">
    <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "Add a new review", class: "btn btn-primary" %>
  <% end %>
  </div>
</div>
